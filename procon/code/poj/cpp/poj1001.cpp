#include<iostream>
#include<cmath>
#include<cstdio>
using namespace std;

int main(){
	double R;
	int n;
	while(cin >> R >> n){
		if(double pow_val = pow(R,n) < 1)
			cout << printf("%f",pow_val) << endl;
		else
			cout << printf("%f",) << endl;
	}
}